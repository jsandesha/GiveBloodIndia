/*
package com.highpeak.gbi.webservices.security;

import com.highpeak.gbi.datastore.model.User;

*/
/**
 * @author sandesha, Created on 22/08/17
 *//*

public class JWTUserFactory {

    private JWTUserFactory()
    {
    }

    public static JWTUser create( User user )
    {
        return new JWTUser(user.getId(), user.getName(), user.getEmailId(), user.getMobileNumber(), user.getGender(),
                user.getBloodGroup(), user.getActive(), user.getLastPasswordResetDate(), user.getCreatedAt());
    }
}
*/
